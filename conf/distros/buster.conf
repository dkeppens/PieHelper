# Debian Buster-specific variable declarations

# Read-only variables

[[ -z "$PH_DISTRO_PKGS" ]] && declare -r PH_DISTRO_PKGS="xserver-xorg-legacy cmake g++ gcc libraspberrypi0 libexpat1 libasound2 libudev0 libudev1 libavahi-client3 libcurl4 libevdev2 libenet7 libraspberrypi0 \
							libssl-dev libopus-dev libasound2-dev libudev-dev libavahi-client-dev libcurl4-openssl-dev libevdev-dev libexpat1-dev libpulse-dev uuid-dev libenet-dev \
							libraspberrypi-dev fakeroot debhelper"

# Read-only exports

export PH_DISTRO_PKGS
