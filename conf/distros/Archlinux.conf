# Archlinux-specific variable declarations

# Read-only variables

[[ -z "$PH_DISTRO_PKGS" ]] && declare -r PH_DISTRO_PKGS="xorg-server cmake opus expat openssl alsa-lib avahi libevdev enet raspberrypi-firmware-tools imx-vpu linux-headers-imx6-cubox-dt"

# Read-only exports

export PH_DISTRO_PKGS
