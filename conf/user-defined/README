# User-defined variables will be loaded from this directory which is defined as variable 'PH_USER_CONF_DIR'
# Variables will only be loaded if they are defined in files ending in extension '.conf'
# Framework variables will override user variables of the same name
# The recommended prefix for user variables to avoid such conflicts can be anything except 'PH_'
