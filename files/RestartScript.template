#!/bin/ksh
# Restart #PH_APP# (by Davy Keppens on 04/10/2018)
# Enable/Disable debug by running confpieh_ph.sh -d restart#PH_APPL#.sh

. $(dirname $0)/../main/main.sh || exit $? && set +x

#set -x

stop#PH_APPL#.sh $1 || exit $?
start#PH_APPL#.sh || exit $?
exit 0
